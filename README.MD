This is my test Symfony api project. It's api for public transport. 

# Run project
On first run:
```bash
$ composer install
```
If you want to load fixtures run:
```bash
$ php bin/console doctrine:fixtures:load
```
# Run phpunit tests
```bash
$ php bin/phpunit tests/
```